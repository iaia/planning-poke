.col.s6.offset-s3
  .card
    .card-content
      = form_with model: User.new, local: true, class: '' do |f|
        .input-field
          = f.text_field :name, class: 'input-field'
          = f.label :name
        = f.submit 'Create User', class: 'btn waves-effect waves-light'
